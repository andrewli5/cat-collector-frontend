import { useState } from "react";
import { Box } from "@mui/material";

export default function SelectProfilePhoto() {
  const [photo, setPhoto] = useState(null);

  return <Box sx={{ border: "1px solid grey" }}>photos here</Box>;
}
